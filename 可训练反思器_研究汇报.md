# 把"产生改进方向"的模块训练起来：可训练反思器（Trainable Reflector）

> **Trainable Reflector for Text-Gradient Optimization** — 面向顶会的研究 idea 汇报
>
> 汇报日期：2026 年 6 月 30 日
>
> 阅读方式：本文**由浅入深**。第 0 章用大白话讲清楚"想做什么、为什么"；第 1–2 章补背景与前作；第 3 章起进入方法、边界、风险与计划。只想快速了解可只读第 0 章。

---

## 第 0 章 · 用最直白的话讲清楚这件事

### 0.1 一个生活类比

想象你在**反复打磨一份简历**：

1. 你拿当前版本去投，收到结果（有没有进面试）——这是**打分**；
2. 有个"职业顾问"看完你的失败，告诉你"你的简历太啰嗦，把项目经历量化一下"——这是**改进方向**；
3. 你照着改，再去投——这是**应用并重试**。

如此循环，简历越来越好。这正是当下一大类 AI 优化方法（GEPA、TextGrad 等）在做的事，只不过被打磨的不是简历，而是**给大模型的提示词（Prompt）**。

### 0.2 当前所有方法的一个共同"偷懒"

在上面的循环里，那个**职业顾问（产生改进方向的角色）**最关键——他说得准，简历才能越改越好。

但现实是：**几乎所有现有方法，都直接找一个"冻结的大模型"来当这个顾问，从来不去训练他。** 他给的建议好不好，全凭运气：有时一针见血，有时空泛无用，有时方向甚至是错的。

### 0.3 本工作想做的一件事

> **把这个"顾问"——也就是产生改进方向的模块——真正训练起来，让他越来越会给有用的建议。**

怎么训练？很简单：**他给的建议，如果照着改之后成绩真的涨了，就奖励他；没涨就惩罚他。** 用强化学习把他练成一个"金牌顾问"。

### 0.4 一张图看懂"谁动谁不动"

| 角色 | 它干什么 | 是否训练 |
|------|---------|---------|
| 任务模型（干活的人） | 用当前提示词去完成任务 | ❌ 不动（可能是闭源模型，改不了） |
| 打分器（考官） | 判断结果好坏，给分数 | ❌ 不动（有标准答案用对错，没有就用大模型打分） |
| **反思器（顾问）** | **看失败，产出"该怎么改"的方向** | ✅ **训练它（核心）** |

记住这一句就够了：

> **别人都在用"冻结的顾问"，我们要把这个顾问训练成专家。**

### 0.5 这件事新不新？（先给个诚实的判断）

- "训练一个模型来优化提示词"这条路**已有前作**（最接近的是 Google 的 **PRewrite**），所以不能简单说"我第一个训练它"。
- 但前作训练的是"**直接改写提示词的盲改器**"——它看不到具体失败、单步改一条通用指令。
- 本工作训练的是"**读具体失败、产出显式改进方向、多步迭代**"的反思器。
- **这就是差异化所在**（详见第 1.4 节）。

读到这里，你已经掌握了整个 idea 的 90%。下面开始逐层加深。

---

## 第 1 章 · 背景与相关工作（补充细节）

### 1.1 什么是"文本空间优化"

大模型时代，提升一个 Agent 的表现有两条路：

- 一条是改**模型权重**（SFT / RL）：成本高、需要可训练的底座；
- 一条是改**文本**：优化提示词（Prompt）、技能文档（Skill）、工作流描述等。

后者不动模型权重，成本低、可解释、可移植，近两年迅速发展为一个独立方向，常被称为**文本空间优化**或**文本梯度**方法。它的通用回路就是第 0.1 节那个"打磨简历"的循环：

```
用当前文本(Prompt)跑任务 → 打分得到反馈 → 反思模块产出"下一步该怎么改"的建议(文本梯度)
        ↑                                                              ↓
        └────────────── 应用建议改写文本 ←── 再次评估 ←──────────────┘
```

> 📌 **术语对齐**：这里的"文本梯度"= 第 0 章里"顾问给的改进方向"。叫"梯度"是因为它类比了神经网络里"指明参数往哪改"的梯度，只不过这里用自然语言表达。

### 1.2 代表性相关工作（谁在做、训不训练顾问）

| 工作 | 核心思想 | "顾问"是否被训练 |
|------|---------|----------------|
| **TextGrad** | 把反向传播类比到文本空间，用 LLM 生成自然语言"梯度"，逐步迭代优化 | ❌ 否（冻结 LLM 直调） |
| **GEPA** | 用反思式 LLM 对失败轨迹做语言反思并进化提示，配合帕累托筛选保留高价值变体 | ❌ 否（冻结 LLM 直调） |
| **DSPy / MIPROv2** | 把 LLM 程序编译化，自动搜索/优化提示与示例 | ❌ 否（搜索 + 冻结 LLM） |
| **SkillOpt (Microsoft)** | 把 Skill 文档当"外部状态"，引入验证门、文本学习率、负反馈缓冲，系统化优化 | ❌ 否（优化器为冻结前沿模型，仅推理） |
| **SkillOS** | 训练一个专门的 Curator，用 RL 学会增/改/删 Skill 库；执行者冻结 | ✅ 是（训练 Curator） |

**一个贯穿全局的观察**：除 SkillOS 外，几乎所有工作都默契地把"产生改进方向"这一最关键环节，**外包给了一个冻结的大模型或闭源 API**。而 SkillOS 在 Skill 管理场景已证明：一个经 RL 训练的 8B Curator，效果可以超过直接调用冻结大模型——这说明"如何产生有效改进"本身是一项**可学习、可优化**的能力。但在 GEPA / TextGrad 这条文本梯度主线上，这件事**还没有人做**。

### 1.3 最相关前作：PRewrite（必须正面对齐）

经全网检索，"用 RL 训练一个模型来优化 prompt（任务模型冻结、用下游表现当奖励）"这条骨架**已有前作**。必须显式对齐，否则会被审稿人直接以"重复工作"打掉。

| 工作 | 训练谁 | 任务模型 | 奖励 | 算法 | 关键特征 |
|------|--------|---------|------|------|---------|
| **RLPrompt** (2022) | 小型 policy network 生成 prompt token | 冻结 | 下游表现 | RL | 输出常为不可读乱码 |
| **StablePrompt / APPO** | agent LLM 自回归生成 prompt | 冻结 | 准确率 / F1 | 改进 PPO | 首次扩到 7B+ agentic LLM |
| **PRewrite** (Google, 2024) | 专门的 Rewriter LLM | 冻结（黑盒 API 可用） | accuracy / EM | PPO + KL | **最接近本想法**：训练重写器、可读、端到端 |

**PRewrite 重点剖析**（与本工作差异最小，必须区分清楚）：

- **做了什么**：训练一个 Rewriter LLM（PaLM 2 底座），把人写的初始指令 `t` 自动改写成更好的 `t†`；任务模型冻结。
- **重写器的输入**：仅 `meta prompt（改写指示）+ 初始指令 t`，**输入里不含失败轨迹**。
- **训练方式**：PPO + KL penalty；状态 = 已解码 token，动作 = 整个词表，奖励 = 改写后指令让冻结任务模型在训练样本上的表现（accuracy / EM）。
- **关键定性**：**input-independent** —— 只优化一条对全任务通用的指令，不针对单条输入或单次失败定制；**单步、一次性，没有显式"文本梯度"环节**。
- **效果**：GSM8K（PaLM 2-L）从初始 prompt 的 37.0% 提升到 PRewrite-S 的 **83.8%**，追平 PromptBreeder（83.9%），超过 APE（77.9）、OPRO（80.2）。

### 1.4 本工作与 PRewrite 的精确边界 ⭐

> ⚠️ **审稿人必问**："这不就是 PRewrite 换个说法？"——以下是标准应答。

| 维度 | PRewrite（已发表） | 本工作 |
|------|-------------------|--------|
| 被训练模型的产物 | 直接产出**新 prompt**（改写器） | 产出**显式文本梯度**（诊断方向），再由应用步得到新 prompt |
| 模型输入 | 元指令 + 旧指令 | **失败轨迹 + 评分反馈** |
| 是否看到具体失败 | ❌ 看不到（盲改通用指令） | ✅ 读具体失败轨迹做针对性诊断 |
| 优化结构 | 单步、一次性 | **多步迭代式文本梯度**（GEPA / TextGrad 式） |
| 输入依赖 | input-independent | 可 input / trace-dependent |
| 信用分配 | 单步奖励 | **多步信用分配**（GAE 式，PRewrite 未涉及） |
| 场景 | 纯 prompt 指令 | 可扩展到 **agentic Skill / 多步工作流进化** |
| 安全性 | 未涉及 | **评判崩塌 + 可验证锚点**防御 |

**一句话区分**：PRewrite 训练的是"直接产出 prompt 的盲改器"（input-independent、单步、看不到失败）；本工作训练的是"显式产出文本梯度的反思器"（读失败轨迹、迭代式、需解决多步信用分配）。两者共享"训练优化器、冻结任务模型、表现当奖励"的骨架，但**优化对象、输入、迭代结构均不同**。

**因此本工作重新定位**：卖点**不是**"我第一个训练 prompt 优化器"（这点 PRewrite 已占），而是：

> **"我第一个在迭代式文本梯度 / agentic 场景下训练显式梯度生成器，并解决 PRewrite 没碰的多步信用分配、评判崩塌与跨任务迁移问题。"**

> 🧪 **必备对照实验**：直接训练"梯度生成器" vs 训练"PRewrite 式直接改写器"——必须证明"显式产生方向再应用"带来可量化优势，否则无法回应"为何不学 PRewrite 直接改 prompt"。

---

### 1.5 两条主线的"文本梯度"究竟怎么算（源码级对比）⭐

> 本节是对 1.2 的技术下钻。**TextGrad 与 GEPA 是文本梯度方向的两个代表，但它们"算梯度"的世界观截然不同。** 看清这一点，才能精确定位"可训练反思器"到底在替换哪个环节、以及为什么这个环节值得训练。下述结论均经其官方源码逐行核对（`zou-group/textgrad`、`gepa-ai/gepa`），非二手转述。

#### 1.5.1 TextGrad：字面模仿反向传播

TextGrad 的核心设计是把神经网络的 autograd **逐一对应**到文本空间：有 `Variable` 对象、有 `.gradients` 集合、有 `backward()`、有梯度累加。一轮优化拆成**两次独立的 LLM 调用**：

1. **backward engine（产梯度）**：对任务输出做批判，生成一段自然语言"梯度"（即"该怎么改"的诊断），作为新 `Variable` 累加进 `variable.gradients`（一个 `set`）。
2. **optimizer LLM（用梯度）**：读取梯度文本，改写变量。

两个关键实现细节值得记住，它们直接暴露了 TextGrad 的脆弱点：

- **聚合 = 纯字符串拼接**。多条梯度的"聚合"就是一行代码，没有任何降噪 / 去重 / 加权：
  ```python
  def get_gradient_text(self) -> str:
      """Aggregates and returns the gradients on a variable."""
      return "\n".join([g.value for g in self.gradients])
  ```
  且 `gradients` 是 `set`，**拼接顺序不保证稳定**。真正"理解这坨拼接梯度"的智能，被外包给了 optimizer LLM 的一次 prompt。
- **optimizer 不是算法，是一段 prompt + 一次 LLM 调用**。`step()` 的核心就是 `engine(...)` 调用 + 用 `<IMPROVED_VARIABLE>` 标签切出新值 + `set_value` **直接覆盖**，无验证、无回退。其 system prompt 甚至明说 *"The feedback may be noisy, identify what is important"*——等于官方承认降噪这件事全甩给了 optimizer 自己。

> **一句话**：TextGrad 把"诊断"与"施工"拆成两次调用，让"梯度"成为可传递、可累加的独立对象；代价是聚合粗糙（无序拼接）、更新无安全网（直接覆盖）。

#### 1.5.2 GEPA：放弃"梯度"，改用反思式进化

GEPA **根本不要"梯度"这个对象**。和 TextGrad 的梯度最对应的东西，是**一次反思变异（reflective mutation）**：读一批轨迹 → 反思 → **直接吐出一版全新的 instruction**。诊断与施工**合并成一次 LLM 调用**。其反思 meta-prompt 的真实结构（`instruction_proposal.py`）为：

```
I provided an assistant with the following instructions:
```<curr_param>```                      ← 当前 instruction（待优化变量）

The following are examples of ... inputs ... response ... and some feedback:
```<side_info>```                        ← 一个 minibatch 的 [输入+输出+反馈] 全塞这

Your task is to write a new instruction for the assistant.
... Identify all niche and domain specific factual information ...
... include generalizable strategy ... Provide new instructions within ``` blocks.
```

关键差异在于 **batch 怎么进入反思**：GEPA **不做任何聚合压缩**，而是把 minibatch 里每条样本用 `convert_sample_to_markdown` 渲染成**结构化 markdown**（`# Example i / ## Inputs / ## Outputs / ## Feedback`），整批原样塞进 `<side_info>`，让反思 LLM 在**完整原始证据**上自己做 credit assignment。

而"坏方向"的防御不靠梯度本身，而靠**进化搜索框架**（Algorithm 1）：反思产出新 instruction → **先在 minibatch 上验证**，`if σ' improved` 才接受、否则**直接丢弃**（绝不污染主候选）→ 再以 **Pareto 前沿**维持多样性、避免局部最优。这正是 TextGrad 缺失的"trust region + 拒绝劣化"。

> **一句话**：GEPA 把一个 minibatch 的**原始三元组结构化铺开**，让 LLM 一次性"反思→写出新 instruction"，把质量控制交给**进化搜索（小批验证 + Pareto 前沿 + 拒绝劣化）**。

#### 1.5.3 对比小结

| 维度 | TextGrad | GEPA |
|------|----------|------|
| 数学类比 | 严格模仿 autograd（Variable / backward / 累加） | 不模仿，是进化 mutation 算子 |
| "梯度"是什么 | 显式对象：一段批评文本 | 无显式梯度，等价物是"一次反思变异" |
| 诊断与施工 | **拆成两次 LLM 调用**（backward 产梯度 → optimizer 用梯度） | **合并为一次调用**（反思即诊断即写新指令） |
| batch 聚合 | `"\n".join`，对**二阶梯度文本**做无序拼接 | 不压缩，把**一阶原始三元组**结构化铺开 |
| 喂给"顾问"的料 | 已抽象过的梯度（二手） | 原始 输入/输出/反馈（一手，带结构） |
| 更新安全网 | 无，`set_value` 直接覆盖 | 有，小批验证 + 不达标即丢弃 + Pareto 维持多样性 |
| 可靠性赌注 | 赌"单次梯度算得准" | 赌"生成得多、但只接受验证过的" |

#### 1.5.4 对本工作的两点直接启示

1. **两者的"顾问"都是冻结 LLM——空白被再次坐实。** 无论 TextGrad 的 backward engine、还是 GEPA 的反思 LLM，**都是冻结直调、从不训练**。本工作要训练的反思器，对应的正是 TextGrad 的 backward engine（产梯度那次调用）/ GEPA 的反思变异调用——这是两条主线共同未触碰的环节。
2. **"如何聚合 / 如何喂料"是已被验证有效的设计变量。** GEPA 仅靠"结构化铺开原始证据 + 验证-拒绝"就显著优于 TextGrad 式无序拼接，说明**反思的输入组织与产出验证本身就携带巨大增益**。这反过来支持本工作的判断：把这一环从"冻结直调"升级为"被训练的策略"，理应有可观空间——且训练时应让反思器直接吃 GEPA 式的结构化原始轨迹（一手料），而非 TextGrad 式的二手梯度。

---

## 第 2 章 · 任务目标

**核心问题：**

> 文本梯度优化中那个"产生改进方向"的反思模块，能不能从"冻结直调"变成"被训练的策略"，从而产出质量更高、更稳定、可迁移的改进方向？

**三条可验证命题（由易到难）：**

1. **命题一（可训练性，地基）**：把反思模块建模为策略 `π_φ`，用 RL 优化"它给出的改进方向被采纳后带来的真实分数提升"，验证该能力**确实可被训练**。
2. **命题二（小胜大，亮点）**：一个 7–8B 的训练后反思器，在标准基准上的优化效果**优于冻结大模型**直调反思。
3. **命题三（可迁移，升华）**：训练好的反思器迁移到新数据集 / 新执行模型时仍然有效，证明它学到的是"**诊断元能力**"而非记忆答案。

**与 GEPA / TextGrad 一类的边界：**

| 维度 | GEPA / TextGrad / SkillOpt | 本工作 |
|------|---------------------------|--------|
| 文本梯度生成器 | 冻结大模型，仅推理 | **可训练策略，RL 更新** |
| 优化信号 | 依赖大模型"凭经验"生成方向 | **以"采纳后真实分数变化"为奖励** |
| 核心贡献类型 | 优化框架 / 工程纪律 | **把优化器本身变成可学习对象** |

---

## 第 3 章 · 为什么会想到这个点

- **从书稿调研中发现空白**：系统梳理 15 篇自进化 Agent 工作后发现，"总结者 / 反思者"这一环几乎无人训练，是被严重低估的模块；而文本梯度主线恰好把这一环固定为冻结大模型。
- **从 SkillOS 的结论受到启发**：SkillOS 证明"训练后的小 Curator > 冻结大模型 Curator"。这一结论天然可迁移到文本梯度场景——如果管理 Skill 可被训练，那么产生文本梯度同样应当可被训练。
- **从现有方法的明显缺陷出发**：冻结大模型生成的改进方向**从未被验证是否真的有用**，常出现"正确但空泛""看似合理却方向错误"的建议。缺少一个以"真实是否带来提升"为目标的优化闭环。
- **与自身能力契合**：该问题本质是"文本空间中的信用分配 + 策略优化"，处在强化学习与提示优化的交叉点，可复用 PPO / GRPO 的方法论积累，形成差异化优势。

---

## 第 4 章 · 方法概述

将文本梯度优化回路中的反思模块替换为**可训练策略**，整体保持"执行—打分—反思—应用—再打分"的闭环，但把奖励反向传播到反思器上。

**三个模块的分工（呼应第 0.4 节）：**

| 模块 | 职责 | 是否训练 |
|------|------|---------|
| 任务模型（Executor） | 用当前 Prompt / Skill 执行任务，产出结果 | ❌ 否（可冻结 / 可闭源） |
| 打分器（Scorer） | 判断结果好坏：有标准答案用对错 / 可验证信号；无标准答案用大模型打分 | ❌ 否 |
| **反思器（Reflector）** | 阅读失败与分数，产出"下一步如何改"的文本梯度 | ✅ **是（RL 训练，核心）** |

**训练闭环（六步）：**

1. 用当前 Prompt 跑任务，打分器给出旧分数 `S_old`；
2. 反思器读取**失败信息**，产出一条改进方向（文本梯度）；
3. 应用改进方向，改写 Prompt / Skill；
4. 用新 Prompt 重跑任务；
5. 用同一个打分器给出新分数 `S_new`；
6. 以 `R = S_new − S_old` 作为奖励，用 RL 更新反思器（分数涨则奖励，不涨则惩罚）。

**关键澄清（最易混淆点）**：打分器**始终不被训练**；它在这里的角色是"给反思器发奖励的工具"。有标准答案时奖励干净可靠，无标准答案时用大模型打分（有噪声，需要可验证锚点兜底）。**真正被训练的只有反思器。**

---

## 第 5 章 · 潜在风险与挑战

| 风险 | 说明 | 应对方向 |
|------|------|---------|
| **奖励信号弱 / 稀疏** | 单轮优化的分数变化常很小且带噪，RL 可能训不动——**最可能导致失败的点** | 优先选可验证、信号干净的任务（代码 / 数学）做排雷；用相对优势归一化、轨迹聚合降噪 |
| **评判者崩塌** | 无标准答案时用大模型打分，反复自循环会把评判偏差固化放大（reward hacking） | 引入可验证锚点（执行器 / 单元测试 / 可验证答案）周期校准；监测自评分与外部验证的散度 |
| **与直接训练任务模型的对比质疑** | 审稿人会问：为何不直接 RL 训练任务模型，而绕道训练反思器 | 强调任务模型冻结 / 闭源不可改、反思器一次训练可跨任务复用等不可替代价值 |
| **与 SkillOpt / GEPA 撞车** | 易被判为"换个封装"的增量工作 | 死咬差异点：**优化器 / 反思模块本身被训练**，而非冻结直调 |
| **与 PRewrite 撞车（最高危）** | PRewrite 已用 RL 训练 prompt 优化器，易被判为重复 | 强调差异：显式文本梯度 vs 直接改写、读失败轨迹 vs 盲改、多步迭代 + 信用分配 vs 单步；并提供对照实验 |
| **评估不统一** | 该方向基准零散、可比性差 | 固定一套基准与协议，公开优化曲线而非仅终值，提供反作弊（防背答案）检测 |

---

## 第 6 章 · 后续推进与优化计划

### 6.1 第一阶段：两周排雷实验（决定是否投入）

- **目标**：在 1–2 个可验证奖励的基准（代码 / 数学优先）上，验证"`S_new − S_old` 这个分差信号是否足以训动反思器"。
- **成立判据**：训练后的小反思器，其优化曲线显著快于 / 高于冻结大模型反思器；若信号被噪声淹没、曲线无差异，则及时止损或调整奖励设计。
- **关键对照组**：同时跑"训练梯度生成器" vs "PRewrite 式直接改写器"，验证显式文本梯度路线的增量价值。
- **产出**：一张"训练反思器 vs 冻结反思器 vs PRewrite 式改写器"的优化曲线对比图 + 简短结论。

### 6.2 第二阶段：方法完善与扩展

- **奖励设计**：从"单步分差"升级为"带衰减的多步贡献"，借鉴 GAE 思路缓解稀疏与短视问题。
- **可验证锚点**：把执行器 / 单元测试纳入奖励，建立评判崩塌的实时监测与回滚机制。
- **跨任务迁移实验**：在任务 A 上训练反思器，迁移到任务 B / 新执行模型，验证诊断元能力。
- **小胜大叙事**：用 7–8B 训练反思器对比强模型冻结反思，争取"小模型超过大模型"的核心结果。

### 6.3 第三阶段：成稿与投稿

- 整理统一评估协议与基准，补齐消融（去奖励整形、去锚点各跑一组）。
- **目标会议**：ICLR / NeurIPS（强结果）；保底 ACL / EMNLP 主会或 findings。
- **与书稿联动**：将该方向沉淀为第 11 章"开放研究方向：训练反思 / 总结模块"的内容，形成研究与著作的互相导流。

---

## 附录 · 一页速记

| 问题 | 一句话回答 |
|------|-----------|
| 想做什么？ | 把"产生改进方向"的反思器从冻结大模型变成被训练的模型 |
| 怎么训练？ | 它给的方向被采纳后分数涨了就奖励，用 RL 优化 |
| 谁不动？ | 任务模型冻结、打分器不训练，只有反思器被训练 |
| 和 PRewrite 啥区别？ | PRewrite 盲改通用指令（单步），本工作读失败轨迹产出显式文本梯度（多步迭代 + 信用分配） |
| 最大风险？ | 奖励信号可能太弱、训不动——先用两周实验排雷 |
| 能发顶会吗？ | 重新定位后可冲 ICLR / NeurIPS，但须用强对照实验挡住"这不就是 PRewrite" |

> **结论**：该 idea 命题清晰、空白真实、与自身能力契合，上限可达顶会；主要风险集中在"奖励信号是否可学"。建议先用**两周最小实验排雷**，信号成立后再全力推进。
