# 总结者：自进化 Agent 里被忽视的关键模块

> 一个常见疑问：现在 Agent 自我进化的研究，似乎都在优化"执行者"的各种 Skill，
> 很少有人去优化"评判 / 总结"这一环——通过提高它来提升整体水平。这个观点站得住吗？
>
> **结论：在"自进化里的总结者 / Curator"这一层，完全站得住。**

---

## 1. 先认识两个角色

把一个会自我进化的 Agent 想象成一个边干活边成长的人，它内部其实有两个不同的角色：

| 角色 | 通俗说法 | 它负责什么 | 现状 |
|------|----------|------------|------|
| **执行者（Executor）** | 干活的人 | 跑任务、改代码、调用工具，直接把事情做完 | 🔥 研究的显学，大家都在拼命优化它的各种 Skill |
| **总结者（Summarizer / Curator）** | 记笔记、整理笔记本的人 | 把干活过程提炼成可复用的经验，并决定哪条经验该增 / 删 / 改 | ❄️ 几乎没人专门研究 |

一句话区分：

> **执行者负责"把这次的活干完"；总结者负责"把这次的经验记下来，让下次干得更好"。**

---

## 2. 总结者在闭环里的位置

```text
┌─────────────────┐      ┌─────────────────┐
│ 执行者（干活）   │ ───→ │ 产生一长串轨迹   │
│ 跑任务/改代码    │      │ 几千字操作记录   │
└─────────────────┘      └────────┬────────┘
        ▲                         │ 浓缩
        │                         ▼
        │                ┌─────────────────┐
        │ 下次开工前       │ 总结者（记笔记） │
        │ 先翻笔记本       │ 提炼成一两句经验 │
        │                │ 并决定增/删/改   │
        │                └────────┬────────┘
        │                         │ 入库
        │                         ▼
        │                ┌─────────────────┐
        └─────────────── │ 技能库 / 经验库  │
                         │"改代码前先读最新 │
                         │  文件"           │
                         └─────────────────┘
```

执行者干完活，会留下一长串详细轨迹（成百上千字的操作记录）。
**总结者**读完这串轨迹，把它浓缩成一两句真正有用的经验（比如"改代码前必须先读最新文件内容"），存进技能库。
下次执行者开工前，先翻一翻这个"笔记本"，于是越用越聪明。

> **关键现象：几乎所有工作里，"总结者"这个角色都被一个现成的大模型直接顶上，没人去专门训练它。**

---

## 3. "训练总结者"到底是什么意思？

这里有两种做法，差别就在于这个"记笔记的人"是**固定的**还是**可以被训练变强的**。

### 做法一（几乎所有工作的选择）：找个现成大模型来记笔记

直接拿 GPT、Claude 这类强模型当总结者，给一段提示词："把这段经历总结成一条经验。"
它怎么总结就怎么总结——**这个能力是固定的、不可改的**。
就像请了个外援帮你记笔记，但你没法让他越记越好。

### 做法二（目前几乎只有 SkillOS 这么做）：专门训练一个小模型，让它越来越会记笔记

用强化学习去**训练**这个总结者：

- 如果它总结出来的经验，让执行者后续表现**变好了** → 给奖励；
- 如果让执行者**变差了** → 给惩罚。

这样它会慢慢学会：**什么样的经验才是真正有用、值得记下来的。**

---

## 4. SkillOS 的关键结论

SkillOS 把这两种做法做了对比，结果出人意料：

> 一个**经过专门训练的小模型（约 80 亿参数）**当总结者，记笔记的水平，
> **反而超过了直接拿来用的、参数大得多的冻结大模型。**

这说明：

> **"怎么记笔记 / 怎么提炼经验"本身，是一项可以专门练、可以变强的本领。**

而绝大多数研究者都默认它是个"现成能力"，随便找个大模型顶上就行，
从没想过"这个角色本身也值得训练"。**这正是被忽视的空白。**

---

## 5. 一个必须配套考虑的反向风险

"优化评判 / 总结者来提升整体"有一个致命前提：**这个角色本身必须比执行者更可靠。**

一旦它有偏差，你越在自进化的闭环里反复"优化"它，
就越会把偏差固化、放大——这就是 **reward hacking / 评估者崩塌**。

在没有外部锚点（代码执行器、单元测试、人工标注、可验证答案）的纯自循环里，
"训练总结者"很容易变成"训练一个越来越自信的错误裁判"。

所以更精确的命题是：

> **在有可信外部验证信号的前提下，把"总结 / 评判这一环"也变成可训练对象，
> 是当前被严重低估的方向。**

---

## 6. 三条可落地的发力方向

1. **填补空白象限**：把 SkillOS 的"训练 Curator"思路，和"自动出题"结合——
   做一个既自动生成任务、又训练总结者的端到端系统（目前没有工作覆盖这个格子）。
2. **横向 + 纵向总结的融合**：现有总结者要么只看单条历史轨迹（纵向），
   要么只看多条采样轨迹（横向），没人把两者融进一个可训练的总结器里。
3. **可信度护栏**：研究"在弱验证信号下如何安全地训练评判者"，
   防止上面两条思路在自循环中崩塌——这本身也是个开放问题。

---

## 一句话总结

- **执行者** = 干活的人（大家都在优化它的 Skill）；
- **总结者** = 记笔记、整理笔记本的人（几乎没人优化它）；
- 你的直觉是：**与其只盯着"干活的人"，不如也练一练"记笔记的人"——
  因为笔记记得好，所有后续的活都会受益。** SkillOS 证明了这条路真的走得通。
